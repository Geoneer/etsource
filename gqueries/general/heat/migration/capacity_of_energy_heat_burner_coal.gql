# The output of this query is, among others, needed to set the new "capacity_of_energy_heat_burner_coal" input.

- unit = MW
- query =
    SUM(
      Q(capacity_of_energy_heater_for_heat_network_coal),
      NEG(Q(capacity_of_industry_heat_burner_coal))
    )
