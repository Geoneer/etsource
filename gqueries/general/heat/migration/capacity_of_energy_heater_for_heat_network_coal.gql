# The output of this query can be, among others, used to set the new "capacity_of_energy_heat_burner_coal" input.

- unit = MW
- query =
    PRODUCT(
      V(energy_heater_for_heat_network_coal, number_of_units),
      V(energy_heater_for_heat_network_coal, heat_output_capacity)
    )
